# AWSEventStream

A pure-Elixir codec for the AWS `vnd.amazon.eventstream` binary protocol.
General-purpose (all header types, encode + decode, CRC32), mirroring
`aws-sdk-go-v2/aws/protocol/eventstream` and botocore. Optional JSON layer.

Status: in development.
